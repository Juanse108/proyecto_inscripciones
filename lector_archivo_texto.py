class LectorArchivoTexto:
    class LectorArchivoTexto:
        def __init__(self, nombreArchivo: str):
            self.nombreArchivo = nombreArchivo

        def obtenerLineas(self) -> list[str]:
            """Lee el archivo y retorna las líneas como lista de strings."""
            pass
